[project]
name = "whats-for-dinner"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pydantic-ai[openai,mcp]",
    "rich",
    "httpx",
    "python-dotenv",
]

[tool.ruff]
line-length = 100

[tool.ruff.lint]
select = ["E", "F", "I"]
